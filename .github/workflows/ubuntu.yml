name: 🐧 OpenMP

on:
  push:
    branches:
      - "development"
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

concurrency:
  group: ${{ github.ref }}-${{ github.head_ref }}-ubuntu
  cancel-in-progress: true

# Configure and build through CTest, so that CDash records their timings as well
# as compiler warnings and errors. Only merges to `development` are submitted;
# PR runs produce the XML locally and drop it.
env:
  CDASH_SITE: GitHub-Actions
  CDASH_SUBMIT: ${{ github.event_name == 'push' && github.ref == 'refs/heads/development' && 'ON' || 'OFF' }}

jobs:

  check_changes:
    name: Analyze
    uses: ./.github/workflows/check_changes.yml

  build_cxxminimal:
    name: GCC Minimal w/o MPI
    runs-on: ubuntu-24.04
    needs: check_changes
    if: ${{ github.event.pull_request.draft == false && needs.check_changes.outputs.has_non_docs_changes == 'true' }}
    env:
      CXXFLAGS: "-Werror"
      CDASH_BUILD_NAME: Linux-GCC-Minimal-RZ3D
      CMAKE_BUILD_PARALLEL_LEVEL: 4
    steps:
    - uses: actions/checkout@v7
    - name: install dependencies
      run: |
        .github/workflows/dependencies/gcc_minimal.sh
    - name: CCache Cache
      uses: actions/cache@v6
      with:
        path: ~/.cache/ccache
        key: ccache-${{ github.workflow }}-${{ github.job }}-git-${{ github.sha }}
        restore-keys: |
             ccache-${{ github.workflow }}-${{ github.job }}-git-
    - name: build WarpX
      run: |
        export CCACHE_COMPRESS=1
        export CCACHE_COMPRESSLEVEL=10
        export CCACHE_MAXSIZE=100M
        ccache -z

        Tools/CI/ctest_dashboard.sh build \
          -DCMAKE_VERBOSE_MAKEFILE=ON  \
          -DWarpX_DIMS="RZ;3"          \
          -DWarpX_EB=OFF               \
          -DWarpX_MPI=OFF              \
          -DWarpX_QED=OFF
        ./build/bin/warpx.3d Examples/Physics_applications/laser_acceleration/inputs_base_3d
        ./build/bin/warpx.rz Examples/Physics_applications/laser_acceleration/inputs_base_rz

        ccache -s
        du -hs ~/.cache/ccache

  build_1D_2D:
    name: GCC 1D & 2D w/ MPI, QED tools
    runs-on: ubuntu-24.04
    needs: check_changes
    if: ${{ github.event.pull_request.draft == false && needs.check_changes.outputs.has_non_docs_changes == 'true' }}
    env:
      CXXFLAGS: "-Werror"
      CXX: "g++-13"
      CC: "gcc-13"
      CDASH_BUILD_NAME: Linux-GCC13-1D2D
      CMAKE_BUILD_PARALLEL_LEVEL: 4
      CMAKE_GENERATOR: Ninja
    steps:
    - uses: actions/checkout@v7
    - name: install dependencies
      run: |
        .github/workflows/dependencies/gcc.sh 13
    - name: CCache Cache
      uses: actions/cache@v6
      with:
        path: ~/.cache/ccache
        key: ccache-${{ github.workflow }}-${{ github.job }}-git-${{ github.sha }}
        restore-keys: |
             ccache-${{ github.workflow }}-${{ github.job }}-git-
    - name: build WarpX
      run: |
        export CCACHE_COMPRESS=1
        export CCACHE_COMPRESSLEVEL=10
        export CCACHE_MAXSIZE=100M
        ccache -z

        Tools/CI/ctest_dashboard.sh build \
          -DCMAKE_VERBOSE_MAKEFILE=ON  \
          -DWarpX_DIMS="1;2"           \
          -DWarpX_EB=OFF               \
          -DWarpX_FFT=ON               \
          -DWarpX_QED_TABLE_GEN=ON     \
          -DWarpX_QED_TOOLS=ON
        ./build/bin/warpx.1d Examples/Physics_applications/laser_acceleration/inputs_base_1d
        ./build/bin/warpx.2d Examples/Physics_applications/laser_acceleration/inputs_base_2d

        ccache -s
        du -hs ~/.cache/ccache

    - name: run QED table tools
      run: |
        .github/workflows/scripts/checkQEDTableGenerator.sh

  build_3D_sp:
    name: GCC 3D & RZ w/ MPI, single precision
    runs-on: ubuntu-24.04
    needs: check_changes
    if: ${{ github.event.pull_request.draft == false && needs.check_changes.outputs.has_non_docs_changes == 'true' }}
    env:
      CXX: "g++-13"
      CC: "gcc-13"
      CDASH_BUILD_NAME: Linux-GCC13-RZ3D-SP
      CMAKE_BUILD_PARALLEL_LEVEL: 4
      CMAKE_GENERATOR: Ninja
    steps:
    - uses: actions/checkout@v7
    - name: install dependencies
      run: |
        .github/workflows/dependencies/gcc_blaspp_lapackpp.sh 13
    - name: CCache Cache
      uses: actions/cache@v6
      with:
        path: ~/.cache/ccache
        key: ccache-${{ github.workflow }}-${{ github.job }}-git-${{ github.sha }}
        restore-keys: |
             ccache-${{ github.workflow }}-${{ github.job }}-git-
    - name: build WarpX
      run: |
        export CCACHE_COMPRESS=1
        export CCACHE_COMPRESSLEVEL=10
        export CCACHE_MAXSIZE=200M
        ccache -z

        # we need to define this *after* having installed the dependencies,
        # because the compilation of blaspp raises warnings.
        export CXXFLAGS="-Werror"

        Tools/CI/ctest_dashboard.sh build     \
          -DCMAKE_VERBOSE_MAKEFILE=ON       \
          -DWarpX_DIMS="RZ;3"               \
          -DWarpX_EB=OFF                    \
          -DWarpX_FFT=ON                    \
          -DWarpX_PRECISION=SINGLE          \
          -DWarpX_PARTICLE_PRECISION=SINGLE \
          -DWarpX_QED_TABLE_GEN=ON
        ./build/bin/warpx.3d Examples/Physics_applications/laser_acceleration/inputs_base_3d
        ./build/bin/warpx.rz Examples/Physics_applications/laser_acceleration/inputs_base_rz

        ccache -s
        du -hs ~/.cache/ccache

  build_gcc_ablastr:
    name: GCC ABLASTR w/o MPI
    runs-on: ubuntu-24.04
    needs: check_changes
    if: ${{ github.event.pull_request.draft == false && needs.check_changes.outputs.has_non_docs_changes == 'true' }}
    env:
      CMAKE_GENERATOR: Ninja
      CXXFLAGS: "-Werror"
      CDASH_BUILD_NAME: Linux-GCC-ABLASTR
      CMAKE_BUILD_PARALLEL_LEVEL: 4
    steps:
    - uses: actions/checkout@v7
    - name: install dependencies
      run: |
        .github/workflows/dependencies/gcc_minimal.sh
        sudo apt-get install -y libopenmpi-dev openmpi-bin
    - name: CCache Cache
      uses: actions/cache@v6
      with:
        path: ~/.cache/ccache
        key: ccache-${{ github.workflow }}-${{ github.job }}-git-${{ github.sha }}
        restore-keys: |
             ccache-${{ github.workflow }}-${{ github.job }}-git-
    - name: build WarpX
      run: |
        export CCACHE_COMPRESS=1
        export CCACHE_COMPRESSLEVEL=10
        export CCACHE_MAXSIZE=100M
        ccache -z

        Tools/CI/ctest_dashboard.sh build \
          -DCMAKE_VERBOSE_MAKEFILE=ON  \
          -DWarpX_APP=OFF              \
          -DWarpX_LIB=OFF

        ccache -s
        du -hs ~/.cache/ccache

  build_pyfull:
    name: Clang pywarpx
    runs-on: ubuntu-24.04
    needs: check_changes
    if: ${{ github.event.pull_request.draft == false && needs.check_changes.outputs.has_non_docs_changes == 'true' }}
    env:
      CC: clang
      CXX: clang++
      CDASH_BUILD_NAME: Linux-Clang-pywarpx
      CMAKE_BUILD_PARALLEL_LEVEL: 4
      # On CI for this test, Ninja is slower than the default:
      #CMAKE_GENERATOR: Ninja
    steps:
    - uses: actions/checkout@v7
    - name: install dependencies
      run: |
        .github/workflows/dependencies/pyfull.sh
    - name: CCache Cache
      uses: actions/cache@v6
      with:
        path: ~/.cache/ccache
        key: ccache-${{ github.workflow }}-${{ github.job }}-git-${{ github.sha }}
        restore-keys: |
             ccache-${{ github.workflow }}-${{ github.job }}-git-
    - name: build WarpX
      run: |
        export CCACHE_COMPRESS=1
        export CCACHE_COMPRESSLEVEL=10
        export CCACHE_MAXSIZE=100M
        ccache -z

        python3 -m pip install --upgrade pip
        python3 -m pip install --upgrade build packaging setuptools[core] wheel

        export CXXFLAGS="-Werror -Wno-error=pass-failed"

        Tools/CI/ctest_dashboard.sh build \
          -DCMAKE_VERBOSE_MAKEFILE=ON  \
          -DWarpX_FFT=ON               \
          -DWarpX_PYTHON=ON            \
          -DWarpX_QED_TABLE_GEN=ON
        # deliberately outside the dashboard build: CTest scrapes the build log
        # for compiler errors, and pip's setuptools warnings
        # ("dist.py:318: UserWarning: ...") match its error pattern
        cmake --build build -j 4 --target pip_install

        ccache -s
        du -hs ~/.cache/ccache

    - name: run pywarpx
      run: |
        export OMP_NUM_THREADS=1
        mpirun -n 2 Examples/Physics_applications/laser_acceleration/inputs_test_3d_laser_acceleration_picmi.py
